(library
  (name parsing)
  (libraries
    core_kernel

    angstrom
    flow_parser
  )
  (preprocess (pps
    ppx_sexp_conv
    ppx_deriving_yojson
  ))
  (ocamlopt_flags -O3 -unbox-closures)
)

(env
  (dev
    (flags (:standard -warn-error -A)))
)
