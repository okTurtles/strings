(library
  (name parsing)
  (libraries
    core_kernel

    angstrom-lwt-unix
    flow_parser
    lwt
    lwt_ppx
  )
  (preprocess (pps
    ppx_sexp_conv
    ppx_deriving_yojson
    lwt_ppx
  ))
  (ocamlopt_flags -O3 -unbox-closures)
)

(env
  (dev
    (flags (:standard -warn-error -A)))
)
